language: c
sudo: false
before_install:
  - source <(curl -SLs https://raw.githubusercontent.com/chatelao/travis-ci-arduino/master/install.sh)
script:
  - build_platform samd:samd:zero
notifications:
  email:
    on_success: change
    on_failure: change
