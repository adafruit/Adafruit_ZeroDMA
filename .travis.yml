language: c
sudo: false
before_install:
  - source <(curl -SLs https://raw.githubusercontent.com/chatelao/travis-ci-arduino/master/install.sh)
script:
  - build_platform arduino_zero_native
notifications:
  email:
    on_success: change
    on_failure: change
